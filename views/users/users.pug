html
  head
    link(rel="stylesheet", href="/styles.css")
  body
    h1 Users
    ul
      each user in users
        li
          a(href=`/users/${user.id}`) #{user.name}
html
  head
    link(rel="stylesheet", href="/styles.css")
  body
    h1 User details
    p ID: #{user.id}
    p Name: #{user.name}
    a(href="/users") Back
